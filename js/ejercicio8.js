let numero
parseInt(numero=prompt("Ingrese un numero para saber si es divisible en 2"))

if(numero % 2 == 0)
    document.write("<p>El numero: "+numero+' si es divisible en 2 </p>')
else
document.write("<p>El numero: "+numero+' no es divisible en 2 </p>')